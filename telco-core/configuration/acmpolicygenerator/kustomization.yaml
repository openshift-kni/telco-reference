generators:
  - core-baseline.yaml
  - core-overlay.yaml
  - core-upgrade.yaml
  - core-finish.yaml
  # Optional: release worker MCPs at the end of an upgrade (include for EUS/double upgrades).
  # - core-upgrade-finish.yaml
  # Optional: pre-cache images ahead of the upgrade window using PinnedImageSet CRs.
  # Label target clusters with upgrade-precache: "" to activate.
  - core-upgrade-precache.yaml

resources:
  - ns.yaml
  - ../template-values
