apiVersion: operators.coreos.com/v1alpha1
kind: CatalogSource
metadata:
  name: redhat-operators-disconnected
  namespace: openshift-marketplace
spec:
  # replace image URL with CatalogSource path generated by mirroring
  # $CATALOG_SOURCE_IMAGE
  image: registry.bastion.example.com:9500/ocp/ocp4/redhat/redhat-operator-index:v4.20
  sourceType: grpc
