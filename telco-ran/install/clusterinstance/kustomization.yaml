apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
- example-sno.yaml
- example-3node.yaml
- example-standard.yaml
# - namespace.yaml
# - secret.yaml
# The ConfigMap referenced by extraManifestsRefs (sno-extra-manifest-configmap) is built
# by the parent kustomization at install/kustomization.yaml using configMapGenerator over
# the extra-manifests/ directory. Run `kustomize build install/` to generate both the
# ClusterInstance examples and the extra-manifests ConfigMap together.
