# optional-extra-manifest (removed from source-crs)

Optional install-time manifests such as `enable-crun-*.yaml` now live under
`../../../install/custom-manifests/`. Example-only content (for example IPsec) remains
under `argocd/example/optional-extra-manifest/`.
